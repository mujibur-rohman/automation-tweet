{
  "name": "automation",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --hot src/index.ts",
    "migrate": "bun run src/db/migrate.ts",
    "submit": "bun run src/intake.ts",
    "sync:once": "bun run src/jobs/sync.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "croner": "^10.0.1",
    "grammy": "^1.43.0"
  }
}
